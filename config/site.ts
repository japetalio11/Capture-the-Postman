export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Capture The Postman",
  description: "A user interface that communicates with a pre-existing backend system for testing API endpoints.",
};
